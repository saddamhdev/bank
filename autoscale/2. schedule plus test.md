চলো এখন দুইটা ফাইল পরিষ্কারভাবে বুঝে নিই 👇
এগুলো দুটোই **Infrastructure Autoscaling Policy** — কিন্তু কাজ আলাদা।

---

# 🔹 1️⃣ autoscale_scheduled.json

এইটা **time-based scaling (Scheduled Scaling)**।

তোমার ফাইলে আছে:

```json
[
  {
    "enabled": true,
    "execute_at": "2023-08-09T08:00:00-04:00",
    "instance_limits": {
      "min": 1,
      "max": 5
    },
    "recurrence": 62
  },
  {
    "enabled": true,
    "execute_at": "2023-08-09T20:00:00-04:00",
    "instance_limits": {
      "min": 1,
      "max": 1
    },
    "recurrence": 127
  }
]
```

---

## 🔥 এটা কী করছে?

এটা বলছে:

### 🕗 সকাল ৮টায়:

* min instance = 1
* max instance = 5

মানে অফিস টাইমে বেশি scale করতে পারবে।

---

### 🌙 রাত ৮টায়:

* min = 1
* max = 1

মানে night mode → fixed single instance।

---

## 🧠 `recurrence` মানে কী?

এটা scheduling repeat interval (platform specific code)
যেমন:

* daily repeat
* weekly repeat
* cron-like repeat

এটা platform interpret করে।

---

## 🏦 Real Use Case

Banking system এ:

| সময়         | Traffic |
| ----------- | ------- |
| Office hour | High    |
| Night       | Low     |

তাই scheduled scaling ব্যবহার করা হয়।

---

# 🔹 2️⃣ autoscale_test.json

এইটা test / lower environment scaling।

তোমার ফাইলে আছে:

```json
{
  "min_instances": 1,
  "max_instances": 4,
  "enabled": true,
  "relationships": {
    "rules": [
      {
        "enabled": true,
        "type": "memory",
        "min_threshold": 60,
        "max_threshold": 75
      },
      {
        "enabled": true,
        "type": "cpu",
        "max_threshold": 50,
        "min_threshold": 25
      }
    ]
  }
}
```

---

## 🔥 এটা কী করছে?

Test environment এ:

* কম load
* limited scaling
* memory + CPU ভিত্তিক scale

---

# 🧠 Difference: autoscale.json vs autoscale_test.json

| File                     | Environment         |
| ------------------------ | ------------------- |
| autoscale.json           | Production          |
| autoscale_test.json      | Test                |
| autoscale_scheduled.json | Time-based override |

---

# 🏗 Platform কীভাবে ব্যবহার করে?

Platform engine:

1️⃣ Repo scan করে
2️⃣ Environment detect করে
3️⃣ এই JSON parse করে
4️⃣ Kubernetes HPA / Cloud scaling rule বানায়

---

# 🔄 Scheduled vs Metric-based Scaling

| Type         | কী দেখে?             |
| ------------ | -------------------- |
| Scheduled    | সময়                  |
| Metric-based | CPU, Memory, Latency |

---

# 🎯 Summary

এই folder activity হলো:

✔ Minimum & maximum pod define করা
✔ Metric-based scaling rule
✔ Time-based scaling rule
✔ Different environment config
✔ Infra-level auto scaling control

---

# 📌 Important

এগুলো Spring Boot bean না
এগুলো Kubernetes / Cloud autoscaler control করে।

---

তুমি চাইলে আমি এখন explain করতে পারি:

* Exact Kubernetes HPA YAML mapping
* Dynatrace / Prometheus metric-based autoscaling
* Production autoscaling tuning strategy
* Banking high traffic day scaling architecture

কোনটা deep জানতে চাও?
